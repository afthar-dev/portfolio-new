/**
 * What this site is actually built with — read off package.json and the font
 * declarations in app/layout.tsx, not aspirational.
 */
export const technology = [
  'Next.js',
  'TypeScript',
  'Tailwind CSS',
  'Framer Motion',
  'Locomotive Scroll',
];

export const typography = [
  'Special Gothic Expanded One',
  'Instrument Serif',
  'Inter',
  'Great Vibes',
];

/** Matches the version field in package.json. */
export const version = '0.1.0';

/** Where "local time" is measured from. */
export const timeZone = 'Asia/Kolkata';
